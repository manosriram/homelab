services:
  bazarr:
    image: lscr.io/linuxserver/bazarr:latest@sha256:7d0a091a63889ce1e4ac4c90595ebd2c50ba5a5df7039a4f4d2be6c2aed6d4ae
    container_name: bazarr
    env_file:
      - .env
    volumes:
      - ${BAZARR_CONFIG_PATH}:/config
      - ${MOVIES_MEDIA_PATH}:/movies
      - ${TV_MEDIA_PATH}:/tv
    deploy:
      update_config:
        order: start-first
    ports:
      - 6767:6767
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://127.0.0.1:6767/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 60s

networks:
  arr:
    driver: bridge
