services:
  gatus:
    container_name: gatus
    image: twinproduction/gatus:v5.24.0
    ports:
      - 9900:8080
    env_file:
      - .env
    volumes:
      - ${GATUS_CONFIG_PATH}:/config
      - ${GATUS_DATA_PATH}:/data
    restart: always
