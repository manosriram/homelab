services:
  jellyseerr:
    image: fallenbagel/jellyseerr:latest
    container_name: jellyseerr
    env_file:
      - .env
    ports:
      - 5055:5055
    volumes:
      - ${JELLYSEER_DATA_PATH}:/app/config
    restart: always
